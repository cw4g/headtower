/**
 * Users views' one-line Headscale error copy.
 *
 * Re-exported from the shared classifier so the Users server component and its
 * create-user Server Action can keep importing {@link describeHeadscaleError}
 * from `./errors` unchanged. See `@/lib/headscale/describe`.
 */

export { describeHeadscaleError } from "@/lib/headscale/describe";
